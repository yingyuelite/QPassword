# AGENTS.md

## 0. 项目概述

- 英文名称：QPassword
- 中文名称：七圈密码
- 简介：本应用是一个简单的密码管理工具，提供记录、查询、统计密码的功能。
- 特色：无后端，用户所有数据均保存在应用本地。

## 1. 功能需求

### 1.1 主要功能

- **记录密码**：支持记录用户密码数据到应用中。
  - 当前支持的字段有：标题、创建时间、用户名、密码、登录方式、关联邮箱、关联手机、关联微信、网址、标签、备注、是否置顶、图标等
  - 数据需要加密存储，加密算法采用 AES-256-GCM
  - 支持添加、删除、修改密码
- **查询密码**：支持通过按关键字或标签查询密码数据。
  - 支持关键字检索，检索字段包括：标题、用户名、登录方式、邮箱、手机号、微信、网址、标签、备注。使用模糊匹配
  - 保留检索历史，用户可以查看最近的检索记录
  - 支持按标签检索密码
- **口令保护**：应用需要设置口令，口令正确才能进入密码管理界面。
  - 设置口令时，需要输入两次，两次输入的口令必须一致
  - 支持图案口令和文字口令
  - 口令长度不能小于 4 位，口令越长安全性越高
  - 不要存储口令，口令仅用于生成口令密钥，用来解密数据密钥
  - 数据密钥仅存储于内存中，不可持久化存储，加密后的数据密钥应当持久化存储
- **密码加密**：用户的密码数据需要加密后存储。
  - 加密算法为 AES-256-GCM，nonce 长度为 12 位，随机生成
  - 每次加密都使用随机 nonce ，提高安全性
  - 具体的加解密过程请参考下面的 **数据安全性保障** 部分。
- **导出/导入**：密码可导出为单文件（扩展名：`.qp2`），JSON 格式。
  - 以密文模式导出，`passwords` 字段为加密后的密文字符串，密钥为当前使用的数据密钥，加密的数据密钥会一并导出
  - 以明文模式导出（不推荐），`passwords` 字段为密码对象数组
  - 支持将本地密码文件（扩展名：`.qp2`）导入到应用中
  - 遇到 `id` 相同的数据，比较字段内容是否完全相同，如果不同则覆盖，完全相同则跳过
  - 导入时，如果当前口令和当时导出时的口令不一致，会提示用户输入正确的口令才能导入
  - 导入时，如果当前口令和当时导出时的口令一致，则自动解析导入
  - 导入时，如果导入的是明文密码，则直接解析导入，不需要输入口令
  - 导入成功后，提示用户更新了多少条数据，新增了多少条数据
- **备份/恢复**：密码数据支持通过 WebDAV 协议备份到云盘或者NAS，同时支持从云盘或者NAS恢复密码数据。
  - WebDAV 连接需要在应用中配置，包括服务器地址、用户名、密码。这些配置信息加密存储到应用本地，加密算法为 AES-256-GCM，密钥为数据密钥
  - 当需要备份和恢复数据时，再读取存储的密文配置信息，解密后使用 WebDAV 协议连接到指定的服务器
  - 备份的密码数据需要加密，密钥为当前使用的数据密钥
  - 备份时，同步记录密码最近更新时间和密码数量（元数据），方便用户查看备份状态
  - 备份路径由用户指定，备份文件名为：`QPassword_backup.qp2` ， 元数据文件名为 `QPassword_meta.json`
  - 恢复时，如果当前口令和当时备份时的口令不一致，会提示用户输入正确的口令才能恢复
  - 恢复时，如果当前口令和当时备份时的口令一致，则自动解析恢复
  - 遇到 `id` 相同的数据，比较字段内容是否完全相同，如果不同则覆盖，完全相同则跳过
  - 恢复成功后，提示用户更新了多少条数据，新增了多少条数据

### 1.2 次要功能

- **设置**：应用主题等。
  - 支持切换应用主题，包括：深色、浅色、跟随系统

## 2. 数据安全性保障

### 2.1 关键概念

| 中文名称    | 英文名称               | 长度     | 生成方式                                         | 用途          |
| ------- | ------------------ | ------ | -------------------------------------------- | ----------- |
| 口令      | Passcode           | >=4字节   | 用户输入                                         | 生成**口令密钥**  |
| 随机盐     | Salt               | 32字节   | 安全随机数                                        | 提高安全性       |
| 口令密钥    | Passcode Key       | 32字节   |  使用 PBKDF2-HMAC-SHA256(**口令**, **随机盐**) 算法生成 | 加解密**数据密钥** |
| 数据密钥    | Data Key           | 32字节   | 安全随机数                                        | 加解密用户密码数据   |
| 加密的数据密钥 | Encrypted Data Key | <br /> | 将**数据密钥**使用 AES-256-GCM 算法加密，加密密钥为**口令密钥**   | 保护数据密钥，验证口令 |

### 2.2 加解密逻辑

#### 2.2.1 用户首次使用时

用户创建**口令**，应用会生成一个**随机盐**，**口令密钥**通过 **PBKDF2(HMAC-SHA256)** 算法生成。

`Passcode Key = PBKDF2(password: Passcode, salt: Salt, iterations: 200,000, keyLen: 256)`

PBKDF2 算法的参数说明：

- `password`：用户输入的口令
- `salt`：随机盐，防止彩虹表攻击
- `iterations`：迭代次数，增加计算成本，提高安全性
- `keyLen`：目标密钥长度，这里设置为 256 位（32字节）

**随机盐**和**迭代次数**两个参数需要保存到应用本地，用于下次口令验证。

生成一个随机的32字节数据，作为**数据密钥**。然后使用上面生成的**口令密钥**，对数据密钥进行加密（AES-256-GCM）。 

加密后的数据密钥需要保存到应用本地。

用户的密码数据均使用**数据密钥**进行加密存储。

#### 2.2.2 用户再次使用时

用户输入**口令**，应用会根据口令和之前生成的随机盐，使用相同的算法生成**口令密钥**。然后使用生成的口令密钥，对**加密的数据密钥**进行解密。

如果解密成功，说明口令正确。应用会使用解密后的**数据密钥**，对用户的密码数据进行加解密。

如果解密失败，说明口令错误。应用会提示用户输入正确的口令。

#### 2.2.3 更改口令时

用户输入**新口令**，应用生成**新的随机盐**。然后通过 PBKDF2 算法生成**新的口令密钥**。

使用新的口令密钥，对**数据密钥**进行加密。

加密后的数据密钥重新保存到应用本地。

用户的密码数据无需重新加密，因为**数据密钥**没有改变（因为口令密钥变化了，所以加密后的数据密钥变化了）。

## 3. 技术栈

- **框架**：[Taro 4.x](https://docs.taro.zone/)
- **UI**：React + TypeScript，优先函数组件，避免使用类组件。
- **网络请求**：小程序端使用 [Taro Request](https://docs.taro.zone/docs/apis/network/request/) ，RN端使用 [fetch](https://developer.mozilla.org/en-US/docs/Web/API/Fetch_API/Using_Fetch) 。
- **包管理器**：npm 。不是 yarn，也不是 pnpm 。

## 4. 开发要求

- 不要轻易删除之前的注释，除非注释是错误的或者需要更新完善。
- 需要同时适配**微信小程序端**和 **React-Native 端**。
- 对于实在无法兼容的属性和样式，可以在 `index.rn.scss` 中添加RN的样式，或者在 `index.rn.tsx` 中实现RN的页面组件。其与 `index.scss` 和 `index.tsx` 中的互不干扰。这是解决跨端冲突的保底方案。
- 如果一个组件目录中已经存在 `*.rn.scss` ，则将**公共样式**写入该文件中。将RN不支持而小程序需要使用的样式写入 `*.scss` 文件中。 `*.scss` 文件中将会 `@use *.rn.scss` 文件。
- RN Text 组件不支持 `text-overflow`, `white-space`, `word-break` 三个 css 属性。
- RN CSS 不支持 border{Top,Right,Bottom,Left} 的简写（shorthands）。
- RN CSS 不支持针对某一边设置 style，即 border-bottom-style 会报错，那么 border-bottom: 1px 就需要写成这种形式：border: 0 style color; border-bottom-width: 1px;
- RN CSS `line-height` 需要带单位，例如 `40px`。
- RN CSS `border-radius` 在 RN 中必须是数字，百分比（如 `50%` ）会报错。
- RN CSS 不支持 `box-sizing` （RN 默认就是 border-box 行为，Taro 全局也设置了 `box-sizing: border-box` ）。
- RN CSS 不支持 `position: fixed` ，只支持 `absolute` / `relative` / `static` 。
- RN CSS `display` 只支持 `flex` 和 `none` 两种值，不支持 `block` 等其他值。
- RN 中 View 标签默认主轴方向是 column，如果不将其他端改成与 RN 一致，就需要在所有用到 display: flex 的地方都显式声明主轴方向为 row: `flex-direction: row;` 。使用 flex 布局时，必须显式声明主轴方向，避免两端样式不一致。
- RN 的事件对象没有 `stopPropagation` 方法。改用 `e.target === e.currentTarget` 判断点击的是遮罩本身而非子元素。
- RN 不支持 `overflow-x / overflow-y` 拆分，只支持 `overflow` 单属性。可考虑将组件从 `View` 改为 `ScrollView` （Taro 的 `ScrollView` 组件支持 `scrollY` 属性）
- Taro RN 运行时会自动给每个页面根元素包裹一个隐式 `ScrollView`，如果需要禁用这个隐式 `ScrollView`，需要在 `index.config.ts` 中设置 `disableScroll: true` 来禁用滚动。
- React Native 的 Image 不支持 SVG 格式，只支持 PNG/JPEG 等格式，可考虑将 Icon 改为用 emoji 文字（✏️ 🗑️ 📋）通过 Text 组件渲染。
- RN 的 `background` 只接受一种颜色 `backgroundColor`，不支持 background-image。在 tsx 中需要使用 `style={{ backgroundColor: color }}` 来设置。
- 微信小程序端发起网络请求，只支持这些请求方法：`GET`, `POST`, `PUT`, `DELETE`, `HEAD`, `OPTIONS`, `TRACE`, `CONNECT` 。
- 微信小程序不支持 `PROPFIND`, `MKCOL` 请求方法，所以无法直接使用 WebDAV 协议列出目录文件和创建目录 。不过上传（PUT请求）和读取文件（GET请求）是支持的。
- React Native 端仅支持类选择器，且不支持 *组合器（两个或多个选择器的组合）* ，建议基于 **BEM** 写样式 。
- 每次新增页面，RN 端都需用 --reset-cache 重新构建，否则会命中这个陈旧缓存。
- `showToast` 方法的 `icon` 参数，可指定为 `success`, `error`, `loading`, `none` 中的一个。需要注意的是：如果不选择 `none`，那么 `title` 文本最多显示 7 个汉字长度。如果需要显示更多汉字，`icon` 需要指定为 `none` （即不显示图标） 。

## 5. 开发规范

1. 需要时抽取公共组件，避免单个 `.tsx` 文件过大。
2. 因为 RN 端支持的样式有限，需要采用 **flex 布局**，具体要求参考：[React Native Remind](https://docs.taro.zone/docs/react-native-remind) 。
3. 如果实现功能需要依赖第三方组件或库，请让我决定是否安装，不要自行决定。
4. 按钮等可点击的元素需要添加 `hoverClass="hover-style"` 和 `hoverStyle={{ opacity: 0.8 }}` ，来实现按钮按下去后的样式，其中 `hoverClass` 是小程序端使用的，`hoverStyle` 是 RN 端使用的。

## 6. 风格

1. 界面清爽简洁，易于操作。
2. \#237166 （light theme） 作为主要颜色（primary按钮、高亮文本、标签等），其他颜色根据需要调整，尽量使用同一色系。应用使用的配色方案在 `src/app.scss` 中定义。
3. 小程序端需要适配暗色主题（dark theme），因RN端适配暗色主题比较复杂，所以RN端暂不适配。

